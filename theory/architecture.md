## Radical Cognitive Architecture (operational form)

The architecture separates two roles:

- Environment prediction: predict `E_{t+1}` from recurrent hidden state.
- Self-modeling bottleneck (Module M): reconstruct compressed self-state.

This induces pressure toward stable latent persistence and provides a measurable
signature under CCI.
